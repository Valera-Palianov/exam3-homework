const initialState = {
    pages: ['books', 'authors', 'members']
}

const generalReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state
    }
}

export default generalReducer